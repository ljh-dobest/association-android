<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ike.coalition.platform">


    <uses-permission android:name="android.permission.INTERNET" />
    <!-- if you want to load images from a file OR from the internet -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:name=".App"
        android:icon="@mipmap/rest"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activity.PlatformParticularsActivity"
            android:exported="true"/>
        <activity android:name=".ui.activity.MInPlatformActivity"
            android:exported="true"/>
        <activity android:name=".ui.activity.PlatformRegisteredActivity"
            android:exported="true"/>
        <activity android:name=".ui.activity.FeedForCommentActivity"
            android:exported="true"/>
        <activity android:name=".ui.activity.CommentMessageActivity"
            android:exported="true"/>
        <meta-data
            android:name="design_width"
            android:value="750" />
        <meta-data
            android:name="design_height"
            android:value="1340" />
    </application>

</manifest>